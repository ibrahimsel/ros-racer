| Theme                            | What I noticed                                                                                                                   | Suggestion                                                                                                                                                                                     |
| -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Naming & consistency**         | *Digital Twin* box is generic, but messages still labelled “ThingMessage”.                                                       | Rename box “Ditto Thing (Digital Twin)” so the term matches message names.                                                                                                                     |
| **Missing interfaces**           | • No arrow showing Composer → simulation (if sim is used for dry-run).<br>• No channel for stack/artifact download (S3/Git/OCI). | Add a dashed “artifact pull” edge from Composer (or Compose Plugin) to a *Resource Registry* block.<br>Connect simulation box either as a special Agent/Composer pair or via a `/sim/*` topic. |
| **Health & metrics return path** | You have metrics arrow **ROS Graph → Agent**, but not Agent → Twin/Dashboard.                                                    | Close the loop with a metrics/telemetry arrow Agent → Twin (REST/MQTT).                                                                                                                        |
| **Plugin lifecycle**             | The edge *executes* arrows imply synchronous chain, but Native Plugin might spawn workspace build asynchronously.                | Use a forked edge or “async” label; optionally group plugins in a container with lifecycle states (init/build/launch).                                                                         |
| **Security envelopes**           | Not shown: credentials for Registry, Ditto, dashboard; network separation (overlay vs host).                                     | Add boundary boxes (“Edge Network”, “Control-Plane Network”) and a note on mTLS / JWT between Agent ↔ Twin.                                                                                    |
| **Simulation**                   | Big grey box sits apart—viewers may ask how it’s triggered.                                                                      | Label it “CI/CD Simulation Cluster” and draw trigger arrow from Stack Registry to a *Simulation Runner* node.                                                                                  |
